[package]
name = "clipbox-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared, platform-agnostic Rust core for Clipbox"

[dependencies]
rusqlite = { version = "0.37", features = ["bundled"] }

[lib]
path = "src/lib.rs"
