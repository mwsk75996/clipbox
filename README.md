# Clipbox

Clipbox is a small desktop app that keeps a local history of the text you copy.

It is still early, but the basic idea already works: leave Clipbox running, copy some text, and it is saved locally for later use.

Anything technical can be found in the [`docs`](docs/) folder.
